/target/

# Build output of desktop/scripts/stage_bundle.sh: a copy of feral-core
# plus a ~350MB relocatable CPython with the brain's dependencies
# installed. Regenerated by `npm run stage:bundle`, which `tauri build`
# runs for you, so it is never worth committing.
/resources/
