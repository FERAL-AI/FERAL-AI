# Build and distribution metadata for the FERAL Python node SDK.
# Implements the Hardware Unification Protocol (HUP v1) client-side.
# `python3 -m build` should succeed against this file (metadata is
# self-contained; no private indexes required).

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "feral-node-sdk"
version = "1.0.0"
description = "Official FERAL Hardware Unification Protocol (HUP v1) client SDK for Python."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "FERAL" }]
keywords = ["feral", "hup", "hardware", "websocket", "iot", "robotics"]
classifiers = [
  "Development Status :: 4 - Beta",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: System :: Hardware",
]
dependencies = [
  "websockets>=12",
  "pydantic>=2.8",
  "zeroconf>=0.131",
  "pynacl>=1.5",
]

[project.urls]
Homepage = "https://github.com/feral-ai/feral-nodes"
Specification = "https://github.com/feral-ai/feral-nodes/blob/main/HUP_SPEC.md"
Issues = "https://github.com/feral-ai/feral-nodes/issues"

[project.scripts]
feral-node = "feral_node_sdk.cli:main"

# Test dependencies. This project shipped a committed test suite and no
# way to install what runs it, which is part of why nothing ran it: when
# HUP moved to 1.4.0 these tests kept asserting 1.3.0 and failed on
# every run for weeks, unseen.
#
# pytest-timeout is here because the CI job passes --timeout: a hung
# websocket test should fail the job, not hold a runner for six hours.
[project.optional-dependencies]
dev = [
  "pytest>=8.2,<9.0",
  "pytest-timeout>=2.3,<3.0",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["feral_node_sdk*"]
