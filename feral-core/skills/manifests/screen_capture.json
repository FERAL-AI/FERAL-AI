{
    "skill_id": "screen_capture",
    "version": "1.0.0",
    "author": "feral-core",
    "brand": {
        "name": "Screen Capture",
        "primary_color": "#0EA5E9",
        "secondary_color": "#0369A1",
        "logo_url": "",
        "icon_set": "sf_symbols"
    },
    "description": "Capture desktop screenshots and optionally describe them with a vision model. Uses the macOS 'screencapture' binary on macOS and 'import' / gnome-screenshot / scrot on Linux; on Linux with none of those installed, and on any other OS, the call fails with a message naming what is missing. Scene analysis needs a VLM (OPENAI_API_KEY or FERAL_VLM_PROVIDER); without one the screenshot still comes back and the response carries analysis_error instead of a description.",
    "trigger_phrases": [
        "take a screenshot",
        "analyze my screen",
        "what is on my screen",
        "capture desktop",
        "screen check"
    ],
    "categories": ["vision", "desktop", "assistive"],
    "auth": {
        "type": "none"
    },
    "endpoints": [
        {
            "id": "capture",
            "method": "PYTHON",
            "url": "",
            "description": "Capture a desktop screenshot and optionally describe it with a vision model. Returns the file path plus base64 image bytes. Setting analyze or query needs a configured VLM; when none is configured the image still comes back and analysis_error says so, so do not describe the screen from imagination.",
            "params": [
                {
                    "name": "path",
                    "type": "string",
                    "required": false,
                    "description": "Optional output file path (default /tmp/feral_screen_<ts>.png)"
                },
                {
                    "name": "region",
                    "type": "string",
                    "required": false,
                    "description": "macOS only: x,y,width,height capture region. Ignored on Linux, which always captures the whole root window."
                },
                {
                    "name": "analyze",
                    "type": "boolean",
                    "required": false,
                    "description": "If true, run scene analysis on the captured screenshot"
                },
                {
                    "name": "query",
                    "type": "string",
                    "required": false,
                    "description": "Specific question about the screenshot content"
                },
                {
                    "name": "include_image",
                    "type": "boolean",
                    "required": false,
                    "description": "Include base64 image in response (default true)"
                }
            ],
            "returns_description": "Screenshot metadata, optional base64 image, and optional scene analysis",
            "ui_hint": "detail_card"
        }
    ],
    "flows": [],
    "crons": [],
    "triggers": [],
    "permissions": ["filesystem", "screen", "vision"],
    "requires_daemon": false,
    "max_calls_per_hour": 1500
}
