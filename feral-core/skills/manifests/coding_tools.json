{
    "skill_id": "coding_tools",
    "version": "1.0.0",
    "author": "feral-core",
    "brand": {
        "name": "Coding Tools",
        "primary_color": "#6c5ce7",
        "secondary_color": "#2d3436",
        "logo_url": "",
        "icon_set": "sf_symbols"
    },
    "description": "Software-engineering and general local-computer surface: repository-scoped file edits (read/write/edit with anchor-based replacements), codebase search (grep/glob), shell commands (git, package managers, test/build runners, ad-hoc one-liners), and HTTP fetch for docs/APIs. Shell commands run on the host inside a folder the operator granted; outside every grant they are refused and the operator is asked for access. This is FERAL's single canonical shell + filesystem toolset.",
    "trigger_phrases": [
        "run", "execute", "bash", "shell", "command",
        "read file", "show file", "cat",
        "write file", "create file", "save file",
        "edit file", "replace", "change in file",
        "search", "grep", "find in code",
        "find files", "list files", "glob",
        "fetch", "download", "curl", "get url"
    ],
    "categories": ["system", "development", "utility"],
    "auth": {
        "type": "none"
    },
    "endpoints": [
        {
            "id": "bash",
            "method": "PYTHON",
            "url": "",
            "description": "Execute a shell command and return stdout/stderr. Use for running programs, git, npm, pip, pytest, etc. Runs on the host in `cwd`, which must resolve inside a folder the operator granted; a command whose cwd or path arguments fall outside every grant is refused with `permission_needed` so the operator can grant access.",
            "params": [
                {"name": "command", "type": "string", "required": true, "description": "The shell command to execute"},
                {"name": "cwd", "type": "string", "required": false, "description": "Working directory to run in. Defaults to FERAL_CWD, then FERAL_WORKSPACE, then the brain's own directory. Must resolve inside a granted folder."},
                {"name": "timeout", "type": "integer", "required": false, "description": "Timeout in seconds (default 30)"}
            ],
            "returns_description": "stdout, stderr, exit code, and the execution mode used (host_workspace or docker)",
            "ui_hint": "detail_card",
            "safety_tier": "confirm"
        },
        {
            "id": "read_file",
            "method": "PYTHON",
            "url": "",
            "description": "Read the contents of a file. Returns the text content with line numbers.",
            "params": [
                {"name": "path", "type": "string", "required": true, "description": "Absolute or relative file path"},
                {"name": "offset", "type": "integer", "required": false, "description": "Start reading from this line (1-based)"},
                {"name": "limit", "type": "integer", "required": false, "description": "Maximum number of lines to read"}
            ],
            "returns_description": "File contents with line numbers",
            "ui_hint": "detail_card",
            "safety_tier": "safe",
            "read_only_hint": true
        },
        {
            "id": "write_file",
            "method": "PYTHON",
            "url": "",
            "description": "Create or overwrite a file with the given content.",
            "params": [
                {"name": "path", "type": "string", "required": true, "description": "File path to write to"},
                {"name": "content", "type": "string", "required": true, "description": "The content to write"}
            ],
            "returns_description": "Confirmation of file creation with byte count",
            "ui_hint": "detail_card",
            "safety_tier": "confirm"
        },
        {
            "id": "edit_file",
            "method": "PYTHON",
            "url": "",
            "description": "Search and replace text in a file. Finds the first occurrence of old_text and replaces it with new_text.",
            "params": [
                {"name": "path", "type": "string", "required": true, "description": "File path to edit"},
                {"name": "old_text", "type": "string", "required": true, "description": "Exact text to find (must be unique in file)"},
                {"name": "new_text", "type": "string", "required": true, "description": "Replacement text"}
            ],
            "returns_description": "Confirmation with diff preview",
            "ui_hint": "detail_card",
            "safety_tier": "confirm"
        },
        {
            "id": "grep_search",
            "method": "PYTHON",
            "url": "",
            "description": "Search for a regex pattern in files (ripgrep-backed). Defaults to returning matching FILE NAMES (output_mode='files_with_matches') — cheap and best for narrowing; ask for output_mode='content' only after you know which files matter. Paths are relative to the workspace root.",
            "params": [
                {"name": "pattern", "type": "string", "required": true, "description": "Regex pattern to search for"},
                {"name": "path", "type": "string", "required": false, "description": "Directory or file to search in (default: current directory)"},
                {"name": "include", "type": "string", "required": false, "description": "File glob pattern to include (e.g. '*.py')"},
                {"name": "output_mode", "type": "string", "required": false, "description": "'files_with_matches' (default — file names), 'content' (lines with line numbers), or 'count' (per-file counts). Prefer files_with_matches first to save context."},
                {"name": "head_limit", "type": "integer", "required": false, "description": "Max results (default 250; 0 = unlimited). Truncated responses include pagination.next_offset."},
                {"name": "offset", "type": "integer", "required": false, "description": "Skip first N results before head_limit (paging large result sets)."}
            ],
            "returns_description": "files_with_matches: {mode, files[], total_files}; content: {mode, matches[{file,line,text}], total}; count: {mode, counts[]}. Truncated results include {truncated, pagination}.",
            "ui_hint": "list",
            "safety_tier": "safe",
            "read_only_hint": true
        },
        {
            "id": "glob_search",
            "method": "PYTHON",
            "url": "",
            "description": "Find files matching a glob pattern (ripgrep-backed, respects .gitignore, most-recently-modified first). Useful for discovering project structure. Paths are relative to the workspace root.",
            "params": [
                {"name": "pattern", "type": "string", "required": true, "description": "Glob pattern (e.g. '**/*.py', 'src/**/*.ts')"},
                {"name": "path", "type": "string", "required": false, "description": "Root directory to search from (default: current directory)"},
                {"name": "head_limit", "type": "integer", "required": false, "description": "Max files to return (default 100; 0 = unlimited). Truncated responses set {truncated: true}."}
            ],
            "returns_description": "{files[], total, truncated?} — files relative to workspace root, newest first",
            "ui_hint": "list",
            "safety_tier": "safe",
            "read_only_hint": true
        },
        {
            "id": "web_fetch",
            "method": "PYTHON",
            "url": "",
            "description": "Fetch a URL and return its content as readable text (HTML stripped).",
            "params": [
                {"name": "url", "type": "string", "required": true, "description": "The URL to fetch"},
                {"name": "max_length", "type": "integer", "required": false, "description": "Max characters to return (default 10000)"}
            ],
            "returns_description": "Page content as plain text",
            "ui_hint": "detail_card",
            "safety_tier": "safe",
            "read_only_hint": true
        }
    ],
    "flows": [],
    "crons": [],
    "triggers": [],
    "permissions": ["filesystem", "network", "shell"],
    "requires_daemon": false,
    "max_calls_per_hour": 10000
}
